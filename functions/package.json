{
  "name": "cmfi-replay-functions",
  "description": "Firebase Cloud Functions for CMFI Replay",
  "version": "1.0.0",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "clean": "rm -rf lib"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "stripe": "^22.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.20",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": "20"
  }
}
